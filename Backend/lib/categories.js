// Canonical item categories. The supplier form, the vendor filter and the
// validation schema all read from here so they cannot drift apart.
const CATEGORIES = ['vegetables', 'fruits', 'spices', 'grains', 'dairy', 'others'];

module.exports = { CATEGORIES };
